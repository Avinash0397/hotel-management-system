server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: hotel-service

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: ${SHOW_SQL:false}

jwt:
  secret: ${JWT_SECRET}
